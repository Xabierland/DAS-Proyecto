plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace 'com.xabierland.librebook'
    compileSdk 35

    defaultConfig {
        applicationId "com.xabierland.librebook"
        minSdk 29
        targetSdk 35
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
}

dependencies {
    implementation libs.appcompat
    implementation libs.material
    implementation libs.activity
    implementation libs.constraintlayout

    implementation 'de.hdodenhof:circleimageview:3.1.0'


    implementation "androidx.cardview:cardview:1.0.0"

    implementation libs.androidx.room.runtime
    annotationProcessor libs.androidx.room.compiler

    testImplementation libs.junit
    androidTestImplementation libs.ext.junit
    androidTestImplementation libs.espresso.core

     // OSMdroid (OpenStreetMaps)
    implementation 'org.osmdroid:osmdroid-android:6.1.16'
    implementation 'com.squareup.okhttp3:okhttp:4.9.3'                      // Dependencia para realizar peticiones HTTP
    implementation 'com.google.code.gson:gson:2.10.1'                       // Dependencia para convertir JSON a objetos Java
    implementation 'com.google.android.gms:play-services-location:21.0.1'   // Dependencia para obtener la ubicación del dispositivo
}